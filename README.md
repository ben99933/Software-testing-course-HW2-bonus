# Sudoku TDD Project

這是一個使用 Test-Driven Development (TDD) 方法開發的數獨遊戲引擎。專案實作了數獨的核心功能，包含規則驗證、解題器和謎題生成器。

## 功能特點

- 🎯 完整的數獨規則驗證
- 🧩 高效的數獨解題器
- 🎲 可自訂難度的謎題生成器
- ✅ 使用 TDD 方法確保程式碼品質

## 技術棧

- 語言：JavaScript (Node.js)
- 測試框架：Jest
- 開發方法：Test-Driven Development (TDD)

## 安裝說明

1. 克隆專案到本地：
```bash
git clone https://github.com/ben99933/Software-testing-course-HW2-bonus.git
cd Software-testing-course-HW2-bonus
```

2. 安裝依賴：
```bash
npm install
```

3. 運行測試：
```bash
npm test
```

## 使用方法

### 建立數獨棋盤

```javascript
const { SudokuBoard } = require('./sudoku');

// 創建一個新的數獨棋盤
const grid = [
    [5, 3, 0, 0, 7, 0, 0, 0, 0],
    [6, 0, 0, 1, 9, 5, 0, 0, 0],
    [0, 9, 8, 0, 0, 0, 0, 6, 0],
    [8, 0, 0, 0, 6, 0, 0, 0, 3],
    [4, 0, 0, 8, 0, 3, 0, 0, 1],
    [7, 0, 0, 0, 2, 0, 0, 0, 6],
    [0, 6, 0, 0, 0, 0, 2, 8, 0],
    [0, 0, 0, 4, 1, 9, 0, 0, 5],
    [0, 0, 0, 0, 8, 0, 0, 7, 9]
];

const board = new SudokuBoard(grid);
```

### 驗證數字放置

```javascript
// 檢查在 (row, col) 位置放置數字 num 是否合法
const isValid = board.isValid(0, 2, 4); // 檢查在第一行第三列放置數字 4 是否合法
console.log(isValid); // true 或 false
```

### 解決數獨

```javascript
// 解決數獨
const solved = board.solve();
if (solved) {
    console.log('找到解答：');
    console.log(board.grid);
} else {
    console.log('此數獨無解');
}
```

### 生成新的數獨謎題

```javascript
const { generate } = require('./sudoku');

// 生成一個有 45 個空格的數獨謎題
const { puzzle, solution } = generate(45);

console.log('生成的謎題：');
console.log(puzzle.grid);

console.log('解答：');
console.log(solution.grid);
```

## 專案結構

```
.
├── sudoku.js          # 主要邏輯實作
├── sudoku.test.js     # 測試案例
└── package.json       # 專案配置檔
```

## 開發過程

本專案採用 TDD 開發方法，開發過程分為三個主要階段：

1. **Phase 1: 規則引擎**
   - 實作數獨基本規則驗證
   - 包含行、列、九宮格的規則檢查

2. **Phase 2: 解題器**
   - 使用回溯算法實作解題器
   - 能夠解決任何有解的數獨題目

3. **Phase 3: 生成器**
   - 實作數獨謎題生成器
   - 支援不同難度等級
   - 確保生成的謎題有唯一解

詳細的開發過程和技術細節可以參考 [TDD_Development_Report.md](./TDD_Development_Report.md)。

## 測試覆蓋率

- 語句覆蓋率：> 95%
- 分支覆蓋率：> 90%
- 函數覆蓋率：100%
